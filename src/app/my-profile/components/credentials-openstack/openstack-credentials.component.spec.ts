/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { OpenStackCredentialsComponent } from './openstack-credentials.component';

describe('Component: Openstack Credentials', () => {
  it('should create an instance', () => {
    let component = new OpenStackCredentialsComponent();
    expect(component).toBeTruthy();
  });
});
